server:
  port: 9191

spring:
  application:
    name: API-GATEWAY
  cloud:
    gateway:
      routes:
      - id: USER-SERVICE
        uri: lb://USER-SERVICE
        predicates:
        - Path=/users/**
      - id: IMPRESSION-SERVICE
        uri: lb://IMPRESSION-SERVICE
        predicates:
        - Path=/impression-service/**